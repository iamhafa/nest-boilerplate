export enum SERVER_CONFIG {
  PORT = 8000,
  PREFIX_API = 'api',
  PREFIX_DOCS = 'docs',
}
export enum RESULTS {
  OK = '0',
  NG = '1',
}
export enum STATUSCODE {
  OK = 200,
  NG = 200,
}
export enum PAGINATION {
  PAGE = 1,
  LIMIT = 10,
}
